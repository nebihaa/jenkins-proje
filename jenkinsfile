pipeline {
     agent {docker  'node:7-alpine' }
     stages {
        stage('Test') {
            steps {
                echo $PATH
                bat 'node --version'
            }
        }
    }
}
