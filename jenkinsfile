pipeline {
     agent {
        docker { 
        label 'windows'
        image 'node:7-alpine/powershell' 
        }
    }

    stages {
        stage('Test') {
            steps {
                sh 'node --version'
            }
        }
    }
}
